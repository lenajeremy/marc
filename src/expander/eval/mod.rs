use crate::marc::ast::Program;

pub struct Evaluator {
    
}

impl Evaluator {
    fn new(program: Program) -> Self {
        Self {
            
        }
    }
}

// runtime-ish
fn test() {
    // let e = Evaluator {};
}
